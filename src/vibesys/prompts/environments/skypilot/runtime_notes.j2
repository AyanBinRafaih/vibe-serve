# Remote runtime

Trusted evaluator commands run sequentially on the operator-selected
`{{ profile_name }}` profile. The allocation has {{ nodes }} node(s), each with
{{ accelerators_per_node }} {{ accelerator_type }} accelerator(s). Cluster policy,
credentials, concrete paths, and container launch details remain host-owned.
