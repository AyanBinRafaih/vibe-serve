Remote accuracy and benchmark work is dispatched through the trusted evaluator
commands. Remote profiling is not yet supported. The editor container has no
accelerator or cluster credentials. Read `{{ runtime_container_path }}` for the
resolved runtime capabilities.
