# VibeSys PR screenshots

Before-and-after captures for the seven `AyanBinRafaih` pull requests open in
`uw-syfi/vibesys` at 2026-09-13 16:35 UTC: #737, #696, #665, #661, #659, #658,
and #656.

Captured with Microsoft's `tui-test` against each PR's merge base and head.
Each directory records the exact revisions, terminal geometry, scenario,
commands, and limitations. The indexing changes in #658 and #659 intentionally
preserve the settled UI; their screenshots are visual smoke checks, not
performance measurements.

These review assets are kept outside the product branches.
